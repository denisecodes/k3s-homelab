# Local inventory and config overrides (contain real IPs/tokens)
k3s-config-local.yml
k3s/k3s-config-local.yml
inventory/hosts-local.ini
linux/inventory/hosts-local.ini
argocd/vault/secrets-local.yml
linux/vault/secrets-local.yml

# Ansible vault password files (never commit these)
.vault_pass
.vault_password
*.vault_pass

# Ansible retry files (created on playbook failure)
*.retry

# Ansible temporary files
.ansible/

# Runtime configs generated by GitHub Actions workflows
runtime-k3s-config.yml

# SSH keys (if anyone accidentally drops one in the repo)
*.pem
*.key
id_rsa
id_ed25519
id_ecdsa

# macOS
.DS_Store

# Editor
.vscode/
*.swp
*.swo
